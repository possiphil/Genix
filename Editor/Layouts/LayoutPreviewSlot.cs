namespace Genix.Editor.Layouts
{
    /// <summary>Available layout preview slot values.</summary>
    public enum LayoutPreviewSlot
    {
        /// <summary>Uses the first comparison preview slot.</summary>
        A,
        /// <summary>Uses the second comparison preview slot.</summary>
        B
    }
}
